class TypeTest is
    method validOperations() is
        var x: 5 + 3 * 2
        var y: 10.5 - 2.0
        var flag: true and false
        var result: (x > y) or flag
    end
    
    method correctReturns() is
        return 42
    end
end