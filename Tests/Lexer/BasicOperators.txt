// for basic words and operators

class TestClass is
    method main is
        var a : Integer(10)
        var b : Real(2.5)
        
        if a.Greater(b.toInteger()) then
            return a.Plus(b.toInteger())
        else
            return a.Minus(b.toInteger())
        end
    end
end