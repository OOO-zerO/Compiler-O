// For basic Identifiers and literals

class IdentifierTest is
    var variable1 : Integer(100)
    var _private_var : Real(0.99)
    var flag1 : Boolean(true)
    var flag2 : Boolean(false)
    
    method processData(param1: Integer, param_2: Real) is
        var result : Integer(param1.Mult(param_2.toInteger()))
    end
end