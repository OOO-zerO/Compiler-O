// Test operators 

class Operatorclass OperatorTest is
    method testOperators : Integer is
        var x : Integer(5)
        var y : Integer(3)
        
        // Math operations
        var sum : Integer(x.Plus(y))
        var diff : Integer(x.Minus(y))
        var product : Integer(x.Mult(y))
        var quotient : Integer(x.Div(y))
        
        var equal : Boolean(x.Equal(y))
        var notEqual : Boolean(x.Equal(y).Not())
        var greater : Boolean(x.Greater(y))
        var less : Boolean(x.Less(y))
        
        if sum.Greater(Integer(0)) then
            return Integer(1)  // P
        else 
            if sum.Less(Integer(0)) then
                return Integer(-1)  // N
            else
                return Integer(0)  // 0
            end
        end
    end
end